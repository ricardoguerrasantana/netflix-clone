import styled from 'styled-components/macro';

const Name = styled.p`
  color: #808080;
  font-size: 28px;

  // // these three goes together to make "this effect..." 
  // white-space: nowrap;
  // overflow: hidden;
  // text-overflow: ellipsis;

`;


export default Name;